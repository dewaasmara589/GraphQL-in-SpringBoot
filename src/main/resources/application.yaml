spring:
  application:
    name: graphqljava

  jpa:
    hibernate:
      ddl-auto: create-drop #dont do this in production
    show-sql: true

  graphql:
    graphiql:
      enabled: true

  threads:
    virtual:
      enabled: true